# OBS Online Service Dashboard

Next.js App Router starter with TypeScript, Lucide icons, `@/` imports, and feature-based modules.

## Run

```bash
npm install
npm run dev
```

## Structure

`src/features/<feature>/{api,component,hooks,types}` keeps each business feature self-contained.
